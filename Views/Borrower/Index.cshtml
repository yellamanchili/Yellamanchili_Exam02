@model IEnumerable<Yellamanchili_Exam02.Models.Borrower>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.status)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Borrower_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.status)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Borrower_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.BorrowerID }) |
            @Html.ActionLink("Details", "Details", new { id=item.BorrowerID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.BorrowerID })
        </td>
    </tr>
}

</table>
